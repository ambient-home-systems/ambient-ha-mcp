# Ambient Home Assistant MCP App

Home Assistant App metadata and operator documentation for Ambient MCP.

See [DOCS.md](DOCS.md) for installation, configuration, local validation, and security
limitations. The application uses the same Python implementation and container image as
the standalone deployment; `AMBIENT_RUNTIME_MODE=home_assistant_app` selects Supervisor
authentication at startup.
